// actions can be used to create
// re-usable functionality between 
// your express route controllers

module.exports = (someData) => {
    // do something interesting
    // with the data, if there is data
    // return relevant data
    return someData;
};